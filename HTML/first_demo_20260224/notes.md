When working w/ HTML ALWAYS ALWAYS ALWAYS start with 'index.html'

What is the difference between inline element and blocking?
Inline
Inline takes ONLY as much room on the page as it needs 

Block
Takes up as much room on the page as it possibly can